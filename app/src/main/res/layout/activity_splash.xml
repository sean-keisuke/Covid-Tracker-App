<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SplashActivity">

    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="373dp"
        android:layout_height="423dp"
        android:layout_marginTop="20dp"
        android:src="@drawable/virus_logo"
        app:layout_constraintBottom_toTopOf="@+id/covidTrackerTitle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:contentDescription="@string/covid_app_logo_desc" />

    <TextView
        android:id="@+id/covidTrackerTitle"
        android:layout_width="335dp"
        android:layout_height="94dp"
        android:gravity="center"
        android:text="@string/covid_app_title"
        android:textColor="#555555"
        android:textSize="36sp"
        android:textStyle="bold|italic"
        android:layout_marginTop="3dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/logoImageView" />
</androidx.constraintlayout.widget.ConstraintLayout>